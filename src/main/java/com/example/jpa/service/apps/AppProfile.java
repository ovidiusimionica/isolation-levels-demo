package com.example.jpa.service.apps;

/**
 * Convenience enum for all spring profiles used in this demo. A profile is actually a logical boot "app".
 */
public interface AppProfile {
  String FIRST  = "first";
  String SECOND = "second";
}
